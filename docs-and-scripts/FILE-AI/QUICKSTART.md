# 🚀 البدء السريع - ربط الواتساب في 5 دقائق

## ⚡ الطريقة الأسرع

### من Windows (جهازك الحالي):

#### 1️⃣ نقل الملفات للسيرفر

**الخيار أ - PowerShell (موصى به):**
```powershell
# افتح PowerShell في مجلد المشروع
# عدّل SERVER_IP في الملف أولاً:
notepad Transfer-ToServer.ps1

# ثم شغّل:
.\Transfer-ToServer.ps1
```

**الخيار ب - WinSCP (أسهل):**
1. حمّل WinSCP: https://winscp.net/
2. اتصل بالسيرفر
3. انقل الملفات التالية إلى `/root/`:
   - `noteShami.py`
   - `get_qr.py`
   - `test_evolution.py`
   - `setup_whatsapp.sh`
   - `config.env`

**الخيار ج - SSH Terminal:**
```bash
# من PowerShell أو CMD
scp noteShami.py get_qr.py test_evolution.py setup_whatsapp.sh config.env root@YOUR_SERVER_IP:/root/
```

---

#### 2️⃣ على السيرفر

```bash
# اتصل بالسيرفر
ssh root@YOUR_SERVER_IP

# انتقل للمجلد
cd /root

# فعّل البيئة الافتراضية
source aqar_env/bin/activate

# شغّل سكريبت استخراج QR
python3 get_qr.py
```

**سيبدأ السكريبت بالمحاولة (30 محاولة × 5 ثواني)**

---

#### 3️⃣ مسح QR Code

**بعد نجاح السكريبت:**

1. سيتم حفظ ملف: `qr_code_YYYYMMDD_HHMMSS.txt`

2. **نقل الملف لجهازك:**
   ```bash
   # من PowerShell على جهازك
   scp root@YOUR_SERVER_IP:/root/qr_code_*.txt ./
   ```

3. **تحويل Base64 إلى صورة:**
   - افتح الملف: `notepad qr_code_*.txt`
   - انسخ كل النص
   - افتح: https://base64.guru/converter/decode/image
   - الصق النص واضغط "Decode Image"

4. **مسح QR بالهاتف:**
   - واتساب > الإعدادات > الأجهزة المرتبطة
   - ربط جهاز
   - امسح الـ QR

---

#### 4️⃣ التحقق

```bash
# على السيرفر
python3 test_evolution.py
```

**يجب أن ترى:**
```
✅ Instance متصل وجاهز للإرسال!
```

---

#### 5️⃣ تحديث رقم الواتساب

```bash
# على السيرفر
nano noteShami.py

# ابحث عن السطر:
WHATSAPP_NUMBER = "966XXXXXXXXX@s.whatsapp.net"

# عدّله لرقمك الحقيقي (مع رمز الدولة بدون +):
WHATSAPP_NUMBER = "966501234567@s.whatsapp.net"

# احفظ: Ctrl+O ثم Enter ثم Ctrl+X
```

---

#### 6️⃣ إعادة تشغيل البوت

```bash
systemctl restart aqar_new.service
systemctl status aqar_new.service
```

---

#### 7️⃣ اختبار نهائي

**من تطبيق Flutter:**
- أضف عقار جديد
- يجب أن يصل إلى:
  - ✅ قناة تلغرام (@aqarShami)
  - ✅ رقم الواتساب

---

## 🎉 تم!

نظامك الآن يعمل بالكامل!

---

## 🆘 مشاكل شائعة

### ❌ "فشل الحصول على QR Code"

```bash
# أعد تشغيل Evolution API
docker restart evolution_api

# انتظر 30 ثانية
sleep 30

# حاول مرة أخرى
python3 get_qr.py
```

---

### ❌ "Connection refused"

```bash
# تأكد أن Evolution API يعمل
docker ps | grep evolution

# إذا لم يعمل:
cd /root/evolution-api
docker-compose up -d
```

---

### ❌ "فشل النشر على واتساب"

```bash
# تحقق من الحالة
python3 test_evolution.py

# إذا كانت "close"، أعد مسح QR
python3 get_qr.py
```

---

## 📚 مراجع مفيدة

- 📖 **الدليل الكامل:** [WHATSAPP_SETUP.md](WHATSAPP_SETUP.md)
- 📝 **أوامر سريعة:** [CHEATSHEET.md](CHEATSHEET.md)
- 📚 **التوثيق الشامل:** [BOT_README.md](BOT_README.md)
- 📊 **حالة المشروع:** [PROJECT_STATUS.md](PROJECT_STATUS.md)

---

## 🛠️ أدوات تفاعلية

```bash
# أداة إعداد تفاعلية (قائمة)
./setup_whatsapp.sh

# اختبار شامل
python3 test_evolution.py

# استخراج QR
python3 get_qr.py
```

---

## ⏱️ الوقت المتوقع

- نقل الملفات: **2 دقيقة**
- استخراج QR: **2-5 دقائق** (حسب استجابة السيرفر)
- مسح QR والتحقق: **1 دقيقة**
- تحديث الإعدادات: **2 دقيقة**

**المجموع: 7-10 دقائق** ⚡

---

**بالتوفيق! 🚀**
