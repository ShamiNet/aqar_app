# 🚀 الأوامر التي تحتاج تشغيلها على السيرفر (بالترتيب)

## ✅ تم إنجازه:
- [x] نقل جميع الملفات للسيرفر بنجاح

---

## 📝 الأوامر التي تحتاج تشغيلها الآن:

### 1️⃣ الاتصال بالسيرفر (من PowerShell):
```powershell
ssh root@qaaz.live
# أدخل كلمة المرور
```

---

### 2️⃣ بعد الدخول للسيرفر، شغّل الأوامر التالية:

#### أ) التأكد من الملفات:
```bash
cd /root
ls -lh *.py *.sh *.env
```
يجب أن ترى:
- ✅ get_qr.py
- ✅ test_evolution.py
- ✅ noteShami.py
- ✅ setup_whatsapp.sh
- ✅ config.env

---

#### ب) تفعيل البيئة الافتراضية:
```bash
source aqar_env/bin/activate
```
يجب أن ترى `(aqar_env)` في بداية السطر

---

#### ج) التأكد من Docker:
```bash
docker ps | grep evolution
```
يجب أن ترى `evolution_api` و `Up`

إذا لم يكن يعمل:
```bash
cd /root/evolution-api
docker-compose up -d
sleep 10
docker ps
```

---

#### د) تشغيل سكريبت استخراج QR Code:
```bash
cd /root
python3 get_qr.py
```

**هذا السكريبت سيحاول 30 مرة (كل 5 ثواني) لاستخراج QR Code**

انتظر حتى ترى رسالة نجاح:
```
✅ تم الحصول على QR Code بنجاح!
📄 تم حفظ Base64 في الملف: qr_code_YYYYMMDD_HHMMSS.txt
```

---

### 3️⃣ بعد نجاح استخراج QR:

#### أ) نقل ملف QR لجهازك (من PowerShell - نافذة جديدة):
```powershell
# افتح PowerShell جديد (لا تغلق اتصال SSH)
cd C:\Users\Qualcomm\pro\aqar_app
scp root@qaaz.live:/root/qr_code_*.txt ./
```

---

#### ب) تحويل Base64 إلى صورة:

**الطريقة 1 - عبر الموقع (أسهل):**
1. افتح الملف: `notepad qr_code_*.txt`
2. انسخ **كل** النص
3. افتح: https://base64.guru/converter/decode/image
4. الصق النص في المربع
5. اضغط "Decode Image"
6. ستظهر صورة QR Code

**الطريقة 2 - Python محلياً:**
```powershell
python -c "import base64; open('qr.png','wb').write(base64.b64decode(open('qr_code_XXXXXXX.txt').read()))"
```

---

#### ج) مسح QR بالهاتف:
1. افتح واتساب على هاتفك
2. اذهب: **الإعدادات** → **الأجهزة المرتبطة**
3. اضغط **ربط جهاز**
4. امسح الـ QR Code من الصورة

⏰ **مهم:** QR Code يعمل لمدة دقيقتين فقط! إذا انتهى الوقت، شغّل `python3 get_qr.py` مرة أخرى.

---

### 4️⃣ التحقق من نجاح الربط (على السيرفر):

```bash
python3 test_evolution.py
```

يجب أن ترى:
```
✅ Instance متصل وجاهز للإرسال!
📊 الحالة: open
```

---

### 5️⃣ تحديث رقم الواتساب:

```bash
nano noteShami.py
```

**ابحث عن السطر (~18):**
```python
WHATSAPP_NUMBER = "966XXXXXXXXX@s.whatsapp.net"
```

**عدّله لرقمك الحقيقي:**
```python
WHATSAPP_NUMBER = "966501234567@s.whatsapp.net"  # استبدل بالرقم الفعلي
```

**💡 ملاحظات مهمة:**
- رمز السعودية: `966` (بدون +)
- يجب أن ينتهي بـ `@s.whatsapp.net`
- لا تضع مسافات أو رموز إضافية

**للحفظ:**
- اضغط `Ctrl+O` ثم `Enter`
- ثم `Ctrl+X` للخروج

---

### 6️⃣ إعادة تشغيل البوت:

```bash
systemctl restart aqar_new.service
systemctl status aqar_new.service
```

يجب أن ترى:
```
● aqar_new.service - Aqar Bot
   Active: active (running)
```

---

### 7️⃣ مراقبة البوت (اختياري):

```bash
journalctl -u aqar_new.service -f
```

اضغط `Ctrl+C` للخروج من المراقبة

---

### 8️⃣ الاختبار النهائي:

**من تطبيق Flutter:**
1. أضف عقار جديد
2. يجب أن يصل تلقائياً إلى:
   - ✅ قناة تلغرام: @aqarShami
   - ✅ رقم الواتساب الذي حددته

---

## 🆘 حل المشاكل السريع:

### ❌ "فشل الحصول على QR Code"
```bash
docker restart evolution_api
sleep 30
python3 get_qr.py
```

### ❌ "Instance state: close"
```bash
python3 get_qr.py
# امسح QR جديد
```

### ❌ "فشل النشر على واتساب"
```bash
# تأكد من الرقم صحيح في noteShami.py
nano noteShami.py
# ثم أعد تشغيل
systemctl restart aqar_new.service
```

### ❌ Docker لا يعمل
```bash
cd /root/evolution-api
docker-compose down
docker-compose up -d
sleep 30
docker ps
```

---

## ✅ Checklist سريع:

- [ ] اتصلت بالسيرفر: `ssh root@qaaz.live`
- [ ] فعّلت البيئة: `source aqar_env/bin/activate`
- [ ] Docker يعمل: `docker ps | grep evolution`
- [ ] شغّلت: `python3 get_qr.py`
- [ ] نقلت ملف QR لجهازي
- [ ] حوّلت Base64 لصورة
- [ ] مسحت QR بالهاتف
- [ ] تأكدت من الربط: `python3 test_evolution.py`
- [ ] حدّثت رقم الواتساب في `noteShami.py`
- [ ] أعدت تشغيل البوت: `systemctl restart aqar_new.service`
- [ ] اختبرت بإضافة عقار حقيقي

---

## 🎯 الخلاصة:

**الأمر الأهم الآن:**
```bash
cd /root
source aqar_env/bin/activate
python3 get_qr.py
```

انتظر ظهور QR Code وانتقل للخطوة 3 👆

**وقت التنفيذ: 5-10 دقائق** ⚡

---

**بالتوفيق! 🚀**
