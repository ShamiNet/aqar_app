# ⚡ إعداد سريع لمستخدم Admin

## 🎯 المشكلة

المستخدمون لا يظهرون في صفحة المستخدمين لأن حسابك الحالي ليس admin.

## ✅ الحل السريع (دقيقتان)

### الخطوة 1: افتح Firebase Console

```
https://console.firebase.google.com
```

### الخطوة 2: اذهب إلى Firestore Database

1. اختر مشروعك
2. من القائمة الجانبية: **Firestore Database**
3. ابحث عن collection اسمها: **users**
4. افتح أي مستخدم (يفضل حسابك)

### الخطوة 3: أضف حقل isAdmin

1. اضغط **"Add field"** (أو رمز +)
2. اكتب:
   - **Field**: `isAdmin`
   - **Type**: اختر `boolean`
   - **Value**: ضع علامة ✓ (true)
3. اضغط **Save**

### الخطوة 4: أعد تشغيل التطبيق

```bash
# أوقف التطبيق (Ctrl+C في terminal)
# ثم شغله من جديد:
flutter run
```

### الخطوة 5: اختبر

1. افتح التطبيق
2. سجّل دخول بالحساب اللي عدّلته
3. اذهب إلى **لوحة القيادة**
4. افتح تبويب **"المستخدمين"**
5. يجب أن تظهر قائمة المستخدمين! ✅

---

## 🔍 إذا ما اشتغل

راقب الـ Debug Console في Android Studio/VS Code:

يجب أن تشوف:
```
🌐 [API] Fetching all users from: http://72.60.80.201:3001/api/admin/users
📡 [API] Users response status: 200
✅ [API] Successfully fetched X users
```

إذا شفت:
```
❌ [API] Failed to fetch users: 403
```
معناه لسه التطبيق ما أخذ الـ admin status. **سجّل خروج ودخول مرة ثانية**.

---

## 📊 ملخص

| الخطوة | الوقت المتوقع |
|--------|---------------|
| 1. Firebase Console | 30 ثانية |
| 2. إضافة isAdmin | 30 ثانية |
| 3. إعادة التشغيل | 1 دقيقة |
| 4. الاختبار | 30 ثانية |
| **المجموع** | **~2 دقيقة** |

---

## ✨ بعد الإعداد

الآن يمكنك:
- ✅ عرض جميع المستخدمين
- ✅ حظر/إلغاء حظر المستخدمين
- ✅ مراقبة المحادثات
- ✅ عرض البلاغات
- ✅ التحكم في إعدادات التطبيق
